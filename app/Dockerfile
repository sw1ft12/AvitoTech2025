# Используем базовый образ с Go
FROM golang:1.22-alpine AS builder

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем go.mod и go.sum, чтобы воспользоваться преимуществами кэширования слоев
COPY go.mod go.sum ./

# Загружаем зависимости (для кэширования)
RUN go mod download

# Копируем весь код приложения
COPY . .

# Собираем приложение
RUN go build -o /bin/app ./cmd/main.go

# Создаем минимальный образ для запуска приложения (опционально, но рекомендуется)
FROM alpine:latest

# Копируем исполняемый файл из этапа сборки
COPY --from=builder /bin/app /app/app

# Устанавливаем рабочую директорию для запуска
WORKDIR /app

# Запускаем приложение
CMD ["./app"]

# (Опционально) Открываем порт, если приложение его слушает
EXPOSE 8080
